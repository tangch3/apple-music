{
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "name": "apple-music",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm install && npm start",
    "server": "cd server && npm install && npm start",
    "start": "npm install concurrently && concurrently \"npm run server\" \"npm run client\"",
    "test-server": "cd server && npx mocha",
    "test": "cd server && npx mocha"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
